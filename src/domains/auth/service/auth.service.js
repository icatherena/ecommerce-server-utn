const UserRepository = require('../../user/repository');

module.exports.AuthService = {
    signUp : async () => {
        try {
            
        }
        catch (error) {
            throw error;
        }
    },
    signIn : async () => {
        try {

        }
        catch (error) {
            throw error;
        }
    }
}